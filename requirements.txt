# ParcelPilot AI Support Agent — Python dependencies
# Pinned loosely (>=) for the assessment; tighten before any deploy.

# --- LLM generation providers (both behind src/llm/client.py) ---
# Neither is used for embeddings — embeddings are always local (fastembed).
google-genai>=0.3.0       # Gemini (recommended primary: stable IDs, generous free tier)
groq>=0.11.0              # Groq (fast fallback; note: Groq has NO embeddings API)

# --- Retrieval: lexical + LOCAL CPU embeddings only ---
rank-bm25>=0.2.2
fastembed>=0.3.6          # BAAI/bge-small-en-v1.5 runs on CPU, no API calls

# --- Web / API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- Data extraction / ingestion ---
pdfplumber>=0.11.0
openpyxl>=3.1.5
pandas>=2.2.0

# --- Dev tooling ---
pytest>=8.3.0
ruff>=0.6.0

# NOTE: No LangChain / LlamaIndex / CrewAI or any agent framework — by design.
